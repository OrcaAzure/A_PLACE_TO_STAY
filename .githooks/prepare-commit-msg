#!/bin/sh
# Strip Cursor agent co-author trailer so only human contributors appear on GitHub.
if [ -f "$1" ]; then
  grep -vi '^Co-authored-by: Cursor <cursoragent@cursor.com>' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
